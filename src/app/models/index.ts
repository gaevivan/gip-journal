import { CustomTypes } from "./types";
import { CustomData } from './data';
import { CustomFunctions } from './functions';

export module Models {
    export import Types = CustomTypes;
    export import Data = CustomData;
    export import Functions = CustomFunctions;
}
